#ROBUST DISE CONFIGURATION FILE

#ENV SETUP
THREAD_COUNT=4

# DEALER SETUP 
# If mode is 0 then (t=3,n=5)
# If mode is 1 then (t=16,n=24)
DISTRIBUTION_MODE=1

# Must be comma seperated and coincide with mode choice
#DEALER_IP_ADDRESSES=localhost:1234,localhost:1235,localhost:1236,localhost:1237,localhost:1238
DEALER_IP_ADDRESSES=localhost:1234